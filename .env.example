# Mapbox GL JS access token (optional)
# Get a free token at https://account.mapbox.com/access-tokens/
# The app works without this — the map section will show a placeholder instead.
NEXT_PUBLIC_MAPBOX_TOKEN=
